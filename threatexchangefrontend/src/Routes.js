const root = '/home'
const dashboard = '/dashboard'
const home =`${root}`
const about = `${root}/about`
const contact = `${root}/contact`
const blog = `${root}/blog`
const login = '/login'
const register = '/register'
const forget = '/forget-password'
const resetPassword = '/reset-password'
const verifyEmail = '/message'
const account = '/account'
 
export {root, dashboard, home, about, contact, blog, login, register, forget, resetPassword,verifyEmail,account}