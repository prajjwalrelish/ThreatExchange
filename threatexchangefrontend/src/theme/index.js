import colors from "./palette";
import textVariants from "./textVarient";
import { spacing, breakpoints } from "./measurement";
import buttonVariants from "./buttonVarient";

const theme = {
    colors,
    textVariants,
    spacing,
    breakpoints,
    buttonVariants
}

export default theme